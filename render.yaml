services:
  # Backend Web Service
  - type: web
    name: traceroute-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build:server
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://traceroute-frontend.onrender.com
    
  # Frontend Static Site
  - type: web
    name: traceroute-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_SERVER_URL
        value: https://traceroute-backend-cp1e.onrender.com
